export const curriculum = [
  {
    id: "secure-coding-fundamentals",
    title: "Secure Coding Fundamentals",
    description:
      "Build foundational coding habits that prevent vulnerabilities before they ship to production.",
    targetAudience: "Developers, junior engineers, and career switchers",
    icon: "ShieldCheck",
    tools: ["OWASP ASVS", "Zod", "TypeScript"],
    labs: [
      ["input-validation-basics", "Input Validation Basics", "validate and normalize untrusted form input", "an Express signup handler", "schema-based server validation before business logic", "trusting client-side checks and coercing values inline"],
      ["canonicalization-normalization", "Canonicalization & Normalization", "canonicalize paths and encodings before validation", "a file upload service", "normalize input first and then apply allowlisted validation", "comparing raw user input directly against blocklists"],
      ["output-encoding-templates", "Output Encoding in Templates", "encode untrusted values for the correct rendering context", "a server-rendered checkout page", "context-aware output encoding plus framework auto-escaping", "reusing one generic escape helper everywhere"],
      ["safe-dom-updates", "Safe DOM Updates", "keep client-side rendering free of injection sinks", "a profile customization widget", "bind text through safe DOM APIs and component props", "writing attacker-controlled values into innerHTML"],
      ["authentication-basics", "Authentication Basics", "implement password verification with strong primitives", "an authentication service", "adaptive password hashing with strict comparison", "custom hashing and string comparison shortcuts"],
      ["registration-rate-limits", "Registration & Login Rate Limits", "throttle abusive authentication traffic without blocking real users", "a login API", "rate limits with lockout telemetry and abuse signals", "unlimited retries with only client-side delays"],
      ["session-id-generation", "Session ID Generation", "issue unpredictable session identifiers", "a session middleware layer", "cryptographically secure, rotated session identifiers", "deterministic or timestamp-based tokens"],
      ["session-fixation-defense", "Session Fixation Defense", "rotate sessions after privilege changes", "a web session manager", "issue fresh sessions after login and elevation", "reusing pre-auth session identifiers"],
      ["secure-error-handling", "Secure Error Handling", "return safe user errors while keeping rich internal diagnostics", "an API error layer", "sanitized user-facing errors with server-side correlation IDs", "returning raw stack traces to speed debugging"],
      ["secure-logging-basics", "Secure Logging Basics", "log enough to investigate issues without exposing secrets or PII", "a payment service logging pipeline", "structured redaction before logs are emitted", "logging full request bodies for troubleshooting"],
      ["dependency-boundary-checks", "Dependency Boundary Checks", "treat internal and partner data as untrusted until verified", "an internal event consumer", "validate every message at the service boundary", "skipping validation because the producer is internal"],
      ["least-privilege-defaults", "Least-Privilege Defaults", "choose secure defaults that deny unsafe behavior by default", "a feature flag service", "deny-by-default policies with explicit allow rules", "allowing everything first and patching exceptions later"],
      ["owasp-top10-mapping", "OWASP Top 10 Mapping", "map code changes to the most common modern web risks", "an architecture review board", "translate feature work into explicit OWASP control checks", "treating OWASP as documentation only"],
      ["insecure-deserialization-basics", "Insecure Deserialization Basics", "parse complex input without reviving attacker-controlled objects", "a background job worker", "strict typed parsing with safe serialization formats", "blindly deserializing user-supplied objects"],
      ["path-traversal-defense", "Path Traversal Defense", "resolve user-controlled file access safely", "a download endpoint", "resolve, normalize, and enforce base-directory checks", "substring blocklists for ../ patterns"],
      ["command-injection-guardrails", "Command Injection Guardrails", "keep shell access away from untrusted input", "a report export service", "use safe library APIs and fixed argument lists", "string-building shell commands with user input"],
      ["secrets-in-code-review", "Secrets in Code Review", "keep credentials out of repos and build-time configs", "a repository bootstrap flow", "load secrets from managed stores and environment injection", "checking API keys into source for faster setup"],
      ["memory-safe-string-handling", "Memory-Safe String Handling", "handle lengths and buffers without unsafe assumptions", "a parser utility", "length-aware APIs and bounds-checked parsing", "manual slicing based on assumed delimiters"],
      ["secure-feature-rollouts", "Secure Feature Rollouts", "release sensitive controls with safe fallback and telemetry", "a rollout configuration system", "phased rollout with telemetry and rollback hooks", "big-bang enablement without guardrails"],
      ["threat-modeling-for-devs", "Threat Modeling for Developers", "turn threat-model findings into concrete engineering tasks", "a sprint planning workflow", "derive abuse cases and backlog controls before coding", "leaving threat notes out of the backlog"],
    ],
  },
  {
    id: "web-application-security",
    title: "Web Application Security",
    description:
      "Protect modern web applications from common browser, session, and request-layer attacks.",
    targetAudience: "Frontend, backend, and full-stack engineers",
    icon: "Globe",
    tools: ["OWASP ZAP", "Content Security Policy", "Browser DevTools"],
    labs: [
      ["reflected-xss-prevention", "Reflected XSS Prevention", "encode untrusted search text before rendering HTML responses", "a server-rendered search page", "context-aware output encoding plus framework auto-escaping", "building response HTML with string concatenation"],
      ["stored-xss-comment-moderation", "Stored XSS Comment Moderation", "neutralize persisted comment content before reuse", "a community discussion feed", "sanitized rich-text pipelines with safe renderers", "storing and replaying raw HTML fragments"],
      ["dom-xss-client-rendering", "DOM XSS in Client Rendering", "keep client-side rendering free of DOM injection sinks", "a profile customization widget", "bind text through safe DOM APIs and component props", "writing attacker-controlled values into innerHTML"],
      ["csrf-token-rotation", "CSRF Token Rotation", "protect state-changing requests from cross-site request forgery", "an account settings form", "per-session CSRF tokens with origin validation", "assuming same-origin cookies are enough"],
      ["same-site-cookie-policies", "SameSite Cookie Policies", "set cookie policies that resist CSRF and session leakage", "a session middleware configuration", "SameSite, Secure, and HttpOnly defaults for sensitive cookies", "shipping auth cookies with broad default attributes"],
      ["sql-parameterized-queries", "SQL Parameterized Queries", "parameterize SQL queries that read filtered business data", "a reporting endpoint", "prepared statements with bound parameters", "string interpolation inside WHERE clauses"],
      ["orm-query-builder-safety", "ORM Query Builder Safety", "use ORM query builders without reintroducing injection flaws", "a search repository method", "safe ORM filters and named parameters", "dropping to raw SQL strings for convenience"],
      ["content-security-policy-basics", "Content Security Policy Basics", "deploy CSP headers that constrain injected script execution", "a web application response policy", "strict CSP with nonces and explicit script sources", "wildcard script allowances to avoid breakage"],
      ["clickjacking-frame-ancestors", "Clickjacking & Frame Ancestors", "prevent hostile embedding of sensitive application screens", "an admin portal", "frame-ancestors and X-Frame-Options defenses", "allowing any partner site to embed the app"],
      ["secure-cookie-hardening", "Secure Cookie Hardening", "harden session cookies against theft and downgrade attacks", "a login response pipeline", "secure transport-only cookies with tight scope", "reusing permissive cookies across subdomains"],
      ["file-upload-validation", "File Upload Validation", "validate uploaded files beyond extension checks", "a document upload endpoint", "content-type, magic-byte, and quarantine validation", "trusting filenames and browser MIME types"],
      ["template-injection-defense", "Template Injection Defense", "avoid server-side template injection in dynamic views", "a customizable email template service", "separating trusted templates from untrusted data placeholders", "evaluating user-supplied template expressions"],
      ["open-redirect-controls", "Open Redirect Controls", "block open redirects in login and deep-link flows", "a redirect helper", "allowlisted return destinations with canonical URL checks", "redirecting to arbitrary next parameters"],
      ["ssrf-guardrails", "SSRF Guardrails", "prevent server-side request forgery in outbound fetch features", "a webhook preview service", "allowlisted destinations plus network egress controls", "letting users fetch arbitrary URLs from the backend"],
      ["cors-origin-review", "CORS Origin Review", "configure CORS so trusted clients can call APIs safely", "an API gateway policy", "precise origin allowlists with correct credential rules", "reflecting any Origin header"],
      ["websocket-session-security", "WebSocket Session Security", "bind real-time connections to authenticated session state", "a live collaboration channel", "short-lived auth tokens with server-side channel authorization", "trusting room IDs alone for access control"],
      ["cache-poisoning-defense", "Cache Poisoning Defense", "keep shared caches from serving attacker-influenced content", "an edge caching layer", "normalized cache keys with explicit vary rules", "caching responses that mix user input into keys"],
      ["third-party-script-governance", "Third-Party Script Governance", "control the risk of analytics and marketing scripts", "a production storefront", "strict review, integrity, and consent gating", "dropping remote scripts into templates without review"],
      ["browser-storage-hygiene", "Browser Storage Hygiene", "keep browser-side storage from leaking tokens and secrets", "a single-page application auth layer", "short-lived memory storage with backend session support", "persisting long-lived tokens in localStorage"],
      ["web-error-observability", "Web Error Observability", "capture web attack telemetry without exposing sensitive details", "a frontend error pipeline", "sanitized client telemetry with server correlation IDs", "sending raw stack traces and request bodies to the browser"],
    ],
  },
  {
    id: "code-review-analysis",
    title: "Code Review & Analysis",
    description:
      "Learn how to spot risky changes, triage findings, and leave review feedback that leads to safer code.",
    targetAudience: "Reviewers, staff engineers, and AppSec partners",
    icon: "Search",
    tools: ["Semgrep", "ESLint", "Dependency Scanners"],
    labs: [
      ["auth-bypass-pull-requests", "Auth Bypass in Pull Requests", "spot authorization bypasses hidden in seemingly harmless diffs", "a pull request for an admin controller", "reviewing every trust boundary and policy check touched by the diff", "approving because tests still pass"],
      ["insecure-randomness-review", "Insecure Randomness Review", "detect weak randomness in tokens and identifiers", "a password reset utility", "cryptographically secure random generators for secrets", "using convenience randomness helpers for security tokens"],
      ["taint-flow-review", "Taint Flow Review", "trace untrusted data from entry points to dangerous sinks", "a service layer refactor", "following tainted data through validation and encoding boundaries", "reviewing each helper in isolation"],
      ["regex-dos-review", "Regex DoS Review", "identify regex patterns that can trigger catastrophic backtracking", "an input validation helper", "bounded patterns and safer parsing strategies", "shipping nested quantifiers without adversarial tests"],
      ["static-analysis-basics", "Static Analysis Basics", "interpret static analysis findings without dismissing real defects", "a Semgrep triage session", "using taint-aware findings to drive code fixes", "auto-closing alerts as noisy without reading context"],
      ["dependency-scan-triage", "Dependency Scan Triage", "rank dependency findings by exploitability and reachability", "an npm dependency review", "prioritizing reachable vulnerable packages with upgrade plans", "sorting only by CVSS score"],
      ["code-smell-risk-ranking", "Code Smell Risk Ranking", "separate security-relevant smells from pure maintainability issues", "a large service class review", "ranking smells by boundary exposure and failure mode", "treating every smell as equally urgent"],
      ["unsafe-crypto-review", "Unsafe Crypto Review", "flag weak or homegrown cryptography during review", "a data protection helper", "approved algorithms, libraries, and key handling", "custom encryption utilities for convenience"],
      ["access-control-diff-analysis", "Access Control Diff Analysis", "verify object-level authorization survives refactors", "a REST resource controller diff", "checking resource ownership and tenant guards in every branch", "assuming route middleware covers every case"],
      ["logging-gap-review", "Logging Gap Review", "spot missing audit trails around privileged actions", "an administrative workflow review", "structured audit events for security-sensitive actions", "adding business logs but no security trail"],
      ["secrets-detection-prs", "Secrets Detection in PRs", "catch credentials before they land in version control", "a feature branch review", "blocking secrets with scanners and secure configuration patterns", "removing the secret later after merge"],
      ["review-comments-security", "Review Comments for Security", "write review comments that drive secure fixes instead of vague warnings", "a peer review conversation", "specific remediation guidance tied to risk and code location", "leaving generic 'security?' comments with no direction"],
      ["secure-refactor-triage", "Secure Refactor Triage", "preserve security controls while refactoring legacy code", "a service extraction effort", "tracking security invariants before and after refactors", "rewriting first and rediscovering controls later"],
      ["architecture-hotspots-analysis", "Architecture Hotspots Analysis", "identify modules that deserve deeper review based on change patterns", "a code ownership dashboard", "combining churn, privilege, and exposure signals to target review", "reviewing files only by size or popularity"],
      ["sast-rule-tuning-review", "SAST Rule Tuning Review", "tune static analysis rules to reduce noise without losing signal", "a security tooling pull request", "tight rule patterns backed by exploit examples and tests", "disabling broad rule classes because of a few false positives"],
    ],
  },
  {
    id: "devsecops-practices",
    title: "DevSecOps Practices",
    description:
      "Secure the pipeline, the platform, and the release process that moves code into production.",
    targetAudience: "Platform engineers, SREs, and DevSecOps teams",
    icon: "Workflow",
    tools: ["GitHub Actions", "Trivy", "Policy as Code"],
    labs: [
      ["ci-pipeline-least-privilege", "CI Pipeline Least Privilege", "scope CI identities and tokens to the minimum required permissions", "a GitHub Actions workflow", "short-lived least-privilege credentials per job", "reusing broad repo-admin tokens across every step"],
      ["build-agent-secrets-management", "Build Agent Secrets Management", "handle secrets safely inside build and deploy jobs", "a CI runner configuration", "ephemeral secret injection with masked logs", "writing credentials to plain-text env dumps"],
      ["sbom-generation-review", "SBOM Generation & Review", "generate and review SBOMs as part of release readiness", "a release pipeline", "automated SBOM publication with dependency attestations", "shipping releases without provenance visibility"],
      ["container-base-image-hardening", "Container Base Image Hardening", "choose and maintain hardened container base images", "a Docker build process", "minimal regularly patched base images with pinned digests", "using sprawling latest tags from public registries"],
      ["dockerfile-security-checks", "Dockerfile Security Checks", "review Dockerfiles for privilege and filesystem hardening", "a service container definition", "non-root execution with explicit file ownership", "running as root with copied secrets and caches"],
      ["kubernetes-manifest-security", "Kubernetes Manifest Security", "secure Kubernetes manifests before deployment", "a Helm values review", "tight service accounts, network rules, and pod security settings", "default service accounts and permissive pods"],
      ["iac-scanning", "Infrastructure as Code Scanning", "scan infrastructure definitions for risky cloud defaults", "a Terraform module", "policy scanning and secure baseline modules", "reviewing IaC only after resources are live"],
      ["policy-as-code-guardrails", "Policy as Code Guardrails", "enforce cloud and pipeline guardrails with policy as code", "an admission and CI policy set", "versioned policy checks that fail unsafe changes early", "manual checklist approvals without automated enforcement"],
      ["shift-left-security-tests", "Shift-Left Security Tests", "run security tests early enough to stop regressions before release", "a pre-merge validation suite", "fast targeted security tests on every pull request", "deferring all security testing to the release branch"],
      ["artifact-signing-verification", "Artifact Signing & Verification", "sign and verify build artifacts throughout release promotion", "an artifact repository workflow", "cryptographic signing with verification at every environment hop", "trusting artifact names and tags alone"],
      ["ephemeral-environment-security", "Ephemeral Environment Security", "secure preview and ephemeral environments used for testing", "a review app platform", "isolated short-lived environments with scrubbed data", "shared preview infrastructure backed by production-like secrets"],
      ["dependency-cache-hardening", "Dependency Cache Hardening", "protect build caches from poisoning and stale vulnerable artifacts", "a package cache strategy", "scoped caches with integrity verification and rotation", "cross-project shared caches with no integrity checks"],
      ["release-approval-controls", "Release Approval Controls", "build release approvals that verify security gates and ownership", "a deployment orchestration flow", "risk-based approvals backed by automated evidence", "single-click production deploys with no gate context"],
      ["cloud-workload-identity", "Cloud Workload Identity", "replace static cloud credentials with workload identity federation", "a cloud deployment flow", "federated identity tokens bound to workload claims", "long-lived cloud access keys in CI variables"],
      ["incident-ready-pipelines", "Incident-Ready Pipelines", "capture pipeline evidence that helps incident response and rollback", "a deployment audit trail", "immutable build, deploy, and rollback metadata", "ad hoc release notes without authoritative logs"],
    ],
  },
  {
    id: "api-backend-security",
    title: "API & Backend Security",
    description:
      "Protect API consumers, backend services, and asynchronous workflows from abuse and data exposure.",
    targetAudience: "Backend engineers and platform API teams",
    icon: "Server",
    tools: ["OpenAPI", "JWT Libraries", "API Gateways"],
    labs: [
      ["api-authentication-patterns", "API Authentication Patterns", "choose authentication patterns that fit public and internal APIs", "an API gateway design review", "explicit auth schemes with consistent token handling", "mixing anonymous and privileged flows in the same endpoints"],
      ["authorization-check-order", "Authorization Check Order", "apply authorization before sensitive data fetches and side effects", "a service method handling account updates", "early authorization gates before data access", "checking permissions after the object is already loaded or changed"],
      ["rate-limiting-strategies", "Rate Limiting Strategies", "design rate limiting that resists abuse without breaking legitimate traffic", "a public API edge policy", "identity-aware throttles with abuse telemetry", "one global limit for every caller and action"],
      ["json-input-sanitization", "JSON Input Sanitization", "validate structured JSON payloads before they reach business logic", "a JSON REST handler", "schema validation and strict field handling", "deserializing flexible payloads into trusted objects"],
      ["jwt-validation-basics", "JWT Validation Basics", "validate JWT signatures claims and audiences correctly", "a bearer token middleware", "strict signature issuer audience and expiry checks", "accepting tokens because they parse successfully"],
      ["refresh-token-rotation", "Refresh Token Rotation", "rotate refresh tokens to limit replay and theft impact", "a session refresh endpoint", "single-use rotation with reuse detection", "long-lived reusable refresh tokens"],
      ["oauth-redirect-validation", "OAuth Redirect Validation", "prevent abuse in OAuth login and consent flows", "an OAuth callback controller", "exact redirect URI matching and state validation", "prefix-matching redirect URIs from request parameters"],
      ["service-to-service-auth", "Service-to-Service Auth", "secure machine-to-machine API calls between internal services", "a backend integration layer", "scoped service identity and mutual trust controls", "shared static tokens across services"],
      ["graphql-resolver-authorization", "GraphQL Resolver Authorization", "enforce authorization at GraphQL resolver boundaries", "a GraphQL API", "field-aware authorization in resolvers and loaders", "assuming top-level schema access is enough"],
      ["webhook-signature-verification", "Webhook Signature Verification", "verify webhook origin and replay resistance before processing", "a payment webhook handler", "signed requests with timestamp and replay checks", "trusting IP allowlists alone"],
      ["idempotency-replay-defense", "Idempotency & Replay Defense", "design write APIs that survive retries without duplicating side effects", "a funds transfer endpoint", "idempotency keys tied to caller and intent", "relying on client retry behavior to stay well-behaved"],
      ["job-queue-hardening", "Job Queue Hardening", "secure asynchronous jobs and worker queues against abuse", "a background task processor", "validated job payloads with scoped worker permissions", "letting any producer enqueue arbitrary work"],
      ["multi-tenant-data-isolation", "Multi-Tenant Data Isolation", "keep tenant data isolated across API and persistence layers", "a SaaS account service", "tenant context propagation and per-request isolation checks", "filtering tenant IDs only in the UI layer"],
      ["api-versioning-security", "API Versioning & Security", "roll API versions without reopening old security assumptions", "an API deprecation program", "version-specific policy checks and safe defaults", "copying legacy insecure behavior into new versions"],
      ["backend-abuse-observability", "Backend Abuse Observability", "instrument backend abuse signals that help defenders react quickly", "an API telemetry pipeline", "structured abuse signals rate data and actor context", "only logging success metrics and generic errors"],
    ],
  },
  {
    id: "mobile-client-security",
    title: "Mobile & Client Security",
    description:
      "Protect client-side state, mobile networking, and distributed binaries from common abuse paths.",
    targetAudience: "Mobile engineers and client platform teams",
    icon: "Smartphone",
    tools: ["OWASP MASVS", "Android Keystore", "Charles Proxy"],
    labs: [
      ["secure-storage-basics", "Secure Storage Basics", "store mobile secrets and tokens in protected platform storage", "an Android client session manager", "OS-backed secure storage for sensitive client secrets", "storing auth tokens in plain shared preferences"],
      ["certificate-pinning-decisions", "Certificate Pinning Decisions", "decide when and how to use certificate pinning safely", "a mobile networking stack", "pinned trust with rotation planning and fallback strategy", "hardcoding fragile certificates with no renewal plan"],
      ["mobile-auth-token-handling", "Mobile Auth Token Handling", "handle mobile auth tokens across foreground background and logout flows", "a mobile authentication flow", "short-lived tokens with explicit wipe and reauth behavior", "caching tokens indefinitely across app states"],
      ["reverse-engineering-risk-review", "Reverse Engineering Risk Review", "raise the cost of reverse engineering sensitive client logic", "a production mobile build", "move secrets server-side and harden client exposure points", "assuming compiled binaries hide everything"],
      ["obfuscation-symbol-stripping", "Obfuscation & Symbol Stripping", "use obfuscation and symbol stripping without breaking observability", "an Android release configuration", "targeted obfuscation with secure mapping file handling", "shipping rich debug symbols in production"],
      ["secure-mobile-networking", "Secure Mobile Networking", "protect mobile API calls on hostile networks", "a client networking layer", "TLS validation retries and trust controls that protect data in transit", "disabling strict transport checks to support old proxies"],
      ["local-data-encryption", "Local Data Encryption", "encrypt locally cached sensitive data with sound key handling", "an offline mobile cache", "app-level encryption backed by platform key stores", "custom encryption with keys stored alongside data"],
      ["mobile-logging-redaction", "Mobile Logging Redaction", "keep mobile logs from leaking secrets PII and session context", "a mobile logging framework", "structured redaction and debug-only logging controls", "verbose production logs with request bodies and tokens"],
      ["clipboard-screenshot-hygiene", "Clipboard & Screenshot Hygiene", "reduce accidental data leakage through clipboard and screenshots", "a sensitive mobile workflow", "sensitive-view protections and minimal clipboard exposure", "copying secrets freely and allowing unrestricted screenshots"],
      ["deep-link-validation", "Deep Link Validation", "validate deep links and intents before using client-side parameters", "a mobile navigation handler", "allowlisted schemes hosts and signed parameters", "trusting any deep link data from the device"],
      ["rooted-device-detection", "Rooted Device Detection", "handle rooted or jailbroken device signals proportionally", "a mobile risk engine", "risk-based controls and server-side enforcement for compromised devices", "fully trusting device integrity without additional checks"],
      ["client-side-secret-avoidance", "Client-Side Secret Avoidance", "keep durable secrets out of distributed mobile binaries", "a mobile configuration system", "server-issued short-lived credentials and remote configuration", "embedding API secrets and signing keys in the app"],
      ["biometric-fallback-security", "Biometric Fallback Security", "design biometric login fallbacks that preserve account security", "a biometric unlock flow", "explicit reauthentication rules and secure fallback paths", "treating any device unlock as full account reauth"],
      ["third-party-sdk-review", "Third-Party SDK Review", "review mobile SDKs for privacy storage and network behavior", "a mobile dependency intake process", "SDK review sandboxing and permission minimization", "dropping tracking SDKs in without network or storage review"],
      ["mobile-data-leakage-prevention", "Mobile Data Leakage Prevention", "prevent sensitive mobile data from leaking through sync backups and shares", "a document sharing feature", "classification-aware sharing controls and backup exclusions", "syncing and exporting sensitive data by default"],
    ],
  },
];
